# SmartAdmin Auto-Coding - Claude API Credentials
# Version: 1.0.0
# ⚠️ SECURITY: Do NOT commit real credentials to version control

apiVersion: v1
kind: Secret
metadata:
  name: claude-api-credentials
  namespace: smartadmin
  labels:
    app: smartadmin-auto-coding
    component: secrets
type: Opaque
stringData:
  # TODO: Replace with actual Claude API Key
  api-key: "<YOUR_CLAUDE_API_KEY_HERE>"
  base-url: "https://api.anthropic.com"
  default-model: "claude-sonnet-4.5"
  fallback-model: "claude-haiku-3.5"
