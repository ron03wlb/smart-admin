package {{PACKAGE}};

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * {{MANAGER_CLASS_NAME}} - Manager Layer
 *
 * Purpose: Handle transactional operations for {{SERVICE_CLASS_NAME}}
 * Generated by: smartadmin-manager-extractor skill
 * Generated at: {{TIMESTAMP}}
 *
 * SmartAdmin Architecture:
 * - Controller → Service → Manager → Dao
 * - @Transactional ONLY in Manager layer
 * - Service delegates to Manager for multi-table operations
 *
 * @author {{AUTHOR}}
 * @since {{VERSION}}
 */
@Service
@RequiredArgsConstructor
public class {{MANAGER_CLASS_NAME}} {

    {{SERVICE_DEPENDENCIES}}

    /**
     * Transactional method: {{METHOD_NAME}}
     *
     * Extracted from: {{SERVICE_CLASS_NAME}}.{{ORIGINAL_METHOD_NAME}}()
     * Reason: Ensure @Transactional compliance with ArchUnit rules
     *
     * {{METHOD_DESCRIPTION}}
     *
     * @param {{PARAMETERS}}
     * @return {{RETURN_TYPE}}
     */
    @Transactional(rollbackFor = Throwable.class)
    public {{RETURN_TYPE}} {{METHOD_NAME}}({{PARAMETER_LIST}}) {
        {{METHOD_BODY}}
    }

}
