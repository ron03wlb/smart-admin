<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.system.mfa.dao.MfaAuditLogDao">

    <!-- Select audit logs by employee ID with pagination -->
    <select id="selectByEmployeeId" resultType="net.lab1024.sa.system.mfa.domain.entity.MfaAuditLogEntity">
        SELECT *
        FROM t_mfa_audit_log
        WHERE employee_id = #{employeeId}
          AND deleted = FALSE
        ORDER BY create_time DESC
    </select>

    <!-- Select audit logs by severity with pagination -->
    <select id="selectBySeverity" resultType="net.lab1024.sa.system.mfa.domain.entity.MfaAuditLogEntity">
        SELECT *
        FROM t_mfa_audit_log
        WHERE severity = #{severity}
          AND deleted = FALSE
        ORDER BY create_time DESC
    </select>

    <!-- Select recent audit logs by employee ID (last N days) -->
    <select id="selectRecentByEmployeeId" resultType="net.lab1024.sa.system.mfa.domain.entity.MfaAuditLogEntity">
        SELECT *
        FROM t_mfa_audit_log
        WHERE employee_id = #{employeeId}
          AND create_time >= NOW() - INTERVAL '#{days} days'
          AND deleted = FALSE
        ORDER BY create_time DESC
    </select>

    <!-- Count recent failed MFA verification attempts -->
    <select id="countRecentFailures" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM t_mfa_audit_log
        WHERE employee_id = #{employeeId}
          AND event_type = 'MFA_VERIFY_FAIL'
          AND create_time >= NOW() - INTERVAL '#{minutes} minutes'
          AND deleted = FALSE
    </select>

</mapper>
