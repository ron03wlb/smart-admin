name: concurrency-safety-auditor
version: "2.0.0"
priority: P1
category: quality
status: stable
description: "Concurrency safety audit with ⭐⭐⭐⭐⭐ risk rating"

patterns_detected:
  - "Check-then-act"
  - "Double-checked locking"
  - "ConcurrentHashMap misuse"
  - "Unsafe publication"
  - "Volatile misuse"
  - "Lock ordering"
  - "Thread-local leaks"
  - "Executor shutdown"

risk_formula: "(Probability × 0.4) + (Impact × 0.35) + (Actual Harm × 0.25)"

related_rules:
  - "technology/patterns/P04-concurrency-rules.md"
